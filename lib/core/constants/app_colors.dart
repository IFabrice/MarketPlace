import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF00703C); // Rwanda green
  static const Color primaryDark = Color(0xFF004F2B);
  static const Color primaryLight = Color(0xFF4CAF50);
  static const Color accent = Color(0xFFFFB300); // Gold / MoMo yellow

  // Neutrals
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F3F5);
  static const Color divider = Color(0xFFE0E0E0);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFFADB5BD);

  // Status
  static const Color success = Color(0xFF2E7D32);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFF57C00);
  static const Color info = Color(0xFF1565C0);

  // In Stock / Out of Stock
  static const Color inStock = Color(0xFF2E7D32);
  static const Color outOfStock = Color(0xFFD32F2F);

  // Star rating
  static const Color star = Color(0xFFFFB300);
}
